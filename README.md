net-snmp from http://www.net-snmp.org/ with local WIN32 changes and can also be used with standard EPICS build system

net-snmp code in "vendor" subdirectory, and unmodified version on vendor branch  
